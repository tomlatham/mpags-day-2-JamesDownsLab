#include "CaesarCipher.hpp"

std::string runCaesarCipher(
    const std::string& inputText,
    const bool encrypt){
    if (encrypt){
        return inputText;
    }
    else{
        return inputText;
    }
}