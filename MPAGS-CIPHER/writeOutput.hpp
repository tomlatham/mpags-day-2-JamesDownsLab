#ifndef MPAGSCIPHER_WRITE_OUTPUT_HPP
#define MPAGSCIPHER_WRITE_OUTPUT_HPP

#include <string>
#include <fstream>
#include <iostream>

void write_output(
    const std::string& inputFile,
    const std::string& outputText
);

#endif